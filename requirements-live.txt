# Optional live/paper trade execution via Alpaca.
# NOTE: alpaca-trade-api==3.2.0 (the latest release on PyPI) hard-pins
# websockets<11, while yfinance requires websockets>=13. These two packages
# cannot be installed into the same environment together -- this is an
# upstream conflict in alpaca-trade-api itself, not a version-pinning
# mistake. Install this file in a SEPARATE virtualenv from requirements.txt
# if you need live execution. src/alpaca_handler.py and src/trade_executor.py
# import alpaca_trade_api lazily so the rest of the app (backtesting,
# dashboard) runs fine without this file installed at all.
alpaca-trade-api==3.2.0
